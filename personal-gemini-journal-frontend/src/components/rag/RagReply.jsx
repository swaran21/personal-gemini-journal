import { useState } from 'react';
import { Bot, ChevronDown, ChevronUp, Database, MapPin, Network } from 'lucide-react';

export function RagReply({ message }) {
  const [open, setOpen] = useState(false);
  const mode = message.retrievalMode || 'SEMANTIC_HYBRID'; const ModeIcon = mode.startsWith('TEMPORAL') ? Database : mode.startsWith('LOCATION') ? MapPin : Network;
  return <div className="max-w-[92%] rounded-3xl rounded-tl-none border border-violet-100 bg-white p-5 text-[15px] leading-relaxed text-slate-700 shadow-sm"><div className="mb-3 flex flex-wrap items-center justify-between gap-2"><div className="flex items-center gap-2 text-[11px] font-bold uppercase tracking-[.16em] text-violet-600"><Bot className="h-3.5 w-3.5" />Past Self</div><span className="inline-flex items-center gap-1.5 rounded-full bg-indigo-50 px-2.5 py-1 text-[9px] font-bold uppercase tracking-wider text-indigo-500"><ModeIcon className="h-3 w-3" />{mode.replaceAll('_', ' ')}</span></div><p className="whitespace-pre-wrap">{message.reply}</p>{message.referencedEntries?.length > 0 && <div className="mt-4 border-t border-violet-100 pt-3"><button onClick={() => setOpen(!open)} className="flex w-full items-center justify-between text-xs font-semibold text-violet-600">Referenced memories <span className="flex items-center gap-1">{message.referencedEntries.length}{open ? <ChevronUp className="h-4 w-4" /> : <ChevronDown className="h-4 w-4" />}</span></button>{open && <ul className="mt-3 space-y-2">{message.referencedEntries.map((reference, index) => <li key={`${index}-${reference}`} className="whitespace-pre-wrap rounded-xl bg-gradient-to-r from-violet-50 to-indigo-50 px-3 py-2 text-xs leading-relaxed text-slate-600">{reference}</li>)}</ul>}</div>}</div>;
}
